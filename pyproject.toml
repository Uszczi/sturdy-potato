[project]
name = "sturdy-potato"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dependency-injector>=4.49.1",
    "django>=6.1",
    "django-vite>=3.1.0",
    "djangorestframework>=3.18.0",
    "drf-spectacular>=0.30.0",
    "pydantic>=2.13.4",
    "uvicorn[standard]>=0.34.0",
    "whitenoise>=6.9.0",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
    "django-stubs[compatible-mypy]>=6.1.0",
    "djangorestframework-stubs>=3.18.0",
    "djlint>=1.44.2",
    "factory-boy>=3.3.3",
    "freezegun>=1.5.5",
    "mypy>=2.3.0",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "pytest-django>=4.11.1",
    "pytest-xdist>=3.8.0",
    "ruff>=0.16.3",
    "types-factory-boy>=0.4.1",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "potato.settings"
pythonpath = ["potato"]

[tool.coverage.run]
omit = ["potato/world/*"]

[tool.ruff]
exclude = ["**/migrations/**"]

[tool.ruff.lint.isort]
known-first-party = ["api", "infrastructure", "potato", "serializers", "todo", "web"]

[tool.mypy]
mypy_path = ["potato"]
plugins = ["mypy_django_plugin.main"]
strict = true

[tool.django-stubs]
django_settings_module = "potato.settings"
